{
    "version": 2,
    "buildCommand": "prisma migrate deploy && prisma db seed && npm run build",
    "installCommand": "npm install --force",
    "framework": "nextjs",
    "outputDirectory": ".next"
}  